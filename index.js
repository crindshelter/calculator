function main(){

}

// Adds two numbers
function add(x, y) {
    return x + y;
}

// Subtract two numbers
function subtract(x, y) {
    return x - y; 
}

// Multiplies two numbers
function multiply(x, y) {
    return x * y;
}

// Divides two numbers
function divide(x, y) {
    return x / y;
}

main()